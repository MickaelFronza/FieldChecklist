server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # SPA com react-router - qualquer rota desconhecida cai no index.html
  # (senao um refresh em /vehicles, por exemplo, daria 404 direto no nginx)
  location / {
    try_files $uri $uri/ /index.html;
  }
}
